import React from 'react';

export default () => {
  const cssStyle = {
    position: "relative",
    width: "500px",
    height: "500px",
    backgroundColor: "blue"
  };
  
  return (
    <div style={ cssStyle }>
    
    </div>
  );
}